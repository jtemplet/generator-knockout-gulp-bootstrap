language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-knockout-gulp-bootstrap' ]; then cd .. && eval "mv $currentfolder generator-knockout-gulp-bootstrap" && cd generator-knockout-gulp-bootstrap; fi

